# ckeditor5-build-classic-all-free-plugins

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ckeditor5-build-classic-all-free-plugins` to build the library.

## Running unit tests

Run `nx test ckeditor5-build-classic-all-free-plugins` to execute the unit tests via [Jest](https://jestjs.io).
